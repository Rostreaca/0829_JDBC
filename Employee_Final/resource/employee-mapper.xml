<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="employeeMapper">

<sql id="employeeColumn">
SELECT
       EMP_ID empId
     , EMP_NAME empName
     , SALARY
     , DEPT_TITLE deptCode
     , JOB_NAME jobCode
  FROM
       EMPLOYEE
  JOIN
       DEPARTMENT ON (DEPT_CODE = DEPT_ID)
  JOIN
       JOB USING (JOB_CODE)
 WHERE
       ENT_YN = 'N'
</sql>



<select id="findAll"
resultType="kh.com.model.vo.Employee">
<include refid="employeeColumn"/>
 ORDER
    BY
       HIRE_DATE ASC
</select>


</mapper>